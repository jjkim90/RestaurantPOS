<dx:ThemedWindow x:Class="RestaurantPOS.WPF.Modules.MenuModule.Views.CategoryEditDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
                 xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
                 xmlns:dxe="http://schemas.devexpress.com/winfx/2008/xaml/editors"
                 Title="{Binding Title}"
                 Height="400" Width="500"
                 WindowStartupLocation="CenterOwner"
                 ShowInTaskbar="False">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <dxlc:LayoutControl Grid.Row="0">
            <dxlc:LayoutGroup Orientation="Vertical">
                <dxlc:LayoutItem Label="Category Name:">
                    <dxe:TextEdit Text="{Binding CategoryName, UpdateSourceTrigger=PropertyChanged}" />
                </dxlc:LayoutItem>
                
                <dxlc:LayoutItem Label="Display Order:">
                    <dxe:SpinEdit Value="{Binding DisplayOrder, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                  MinValue="1" 
                                  MaxValue="100" 
                                  IsFloatValue="False"
                                  Increment="1"/>
                </dxlc:LayoutItem>
                
                <dxlc:LayoutItem Label="Active:">
                    <CheckBox IsChecked="{Binding IsActive}" />
                </dxlc:LayoutItem>
            </dxlc:LayoutGroup>
        </dxlc:LayoutControl>

        <StackPanel Grid.Row="1" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <dx:SimpleButton Content="저장" 
                           IsDefault="True"
                           Width="120" 
                           Height="50"
                           FontSize="16"
                           Margin="0,0,10,0"
                           Click="SaveButton_Click"/>
            <dx:SimpleButton Content="취소" 
                           IsCancel="True"
                           Width="120"
                           Height="50"
                           FontSize="16"
                           Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</dx:ThemedWindow>